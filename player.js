class Player {
  constructor(stamina, speed, accel) {
    this.stamina = stamina;
    this.speed = speed;
    this.accel = accel;
  }
}

// 초기 플레이어 (70,60,60)
const mainPlayer = new Player(70, 60, 60);
